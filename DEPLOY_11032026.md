# Déploiement — 11/03/2026

## 1. Télécharger YAMNet sur le Pi

```
ssh pollen@192.168.1.244
```

```
wget https://storage.googleapis.com/download.tensorflow.org/models/tflite/task_library/audio_classification/rpi/lite-model_yamnet_classification_tflite_1.tflite -O /home/pollen/reachy_care/models/yamnet.tflite
```

## 2. Installer tflite-runtime (si absent)

```
/venvs/apps_venv/bin/pip install tflite-runtime
```

## 3. Déployer depuis le Mac

```
bash /Users/alexandre/Galaad-Motokiyo-Ferran/reachy_care/cmd.sh
```

→ option `1`

## 4. Vérifier les logs

```
ssh pollen@192.168.1.244 "tail -f /home/pollen/reachy_care/logs/reachy_care.log"
```

```
ssh pollen@192.168.1.244 "tail -f /home/pollen/reachy_care/logs/conv_app.log"
```

## 5. Vérifier que les fixes openai_realtime.py sont bien patchés

```
ssh pollen@192.168.1.244 "grep -n 'interrupt_response\|server_vad\|reachy-care-events' /home/pollen/reachy_mini_conversation_app/src/reachy_mini_conversation_app/openai_realtime.py | head -10"
```

Doit afficher au moins 3 lignes avec `server_vad` et `interrupt_response`.

## 6. Vérifier que YAMNet se charge (dans les logs)

Chercher dans reachy_care.log :

```
SoundDetector
```

- `SoundDetector: modèle YAMNet chargé` → OK
- `SoundDetector: modèle introuvable` → étape 1 non faite
- `SoundDetector: tflite_runtime non disponible` → étape 2 non faite

## 7. Nettoyer le disque (Pi à 85%)

```
ssh pollen@192.168.1.244 "rm -rf /home/pollen/reachy_care/models/models/ && df -h /home/pollen"
```

---

## Ce qui a changé dans ce déploiement

| Fichier | Changement |
|---|---|
| `modules/memory_manager.py` | Schéma + `add_session()` / `add_facts()` |
| `modules/sound_detector.py` | Nouveau — YAMNet TFLite, thread daemon |
| `main.py` | SoundDetector + `_handle_sound_impact()` + `_summarize_session()` 2 appels GPT + `_handle_face()` contexte enrichi |
| `config.py` | `FALL_GHOST_TRIGGER_SEC` 2.5→5.0 + constantes `SOUND_*` |
| `external_profiles/reachy_care/instructions.txt` | Persona réécrit — plus de maternage |
